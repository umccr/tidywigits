---
title: "Summary"
lightbox:
  match: auto
  effect: fade
execute: 
  echo: false
---

```{r}
#| label: pkg_load
#| message: false
box::use(
  here[here],
  dplyr[mutate, filter, select],
  tibble[tibble] # needed for tbl print
)
options(box.path = here("inst/website/boxbox"))
box::use(
  wigits/plot[cat_plot],
  wigits/utils[get_data],
  purple/qc[qc_tab = qc]
)
```

```{r}
#| label: data_setup
dat <- get_data()
```

```{r}
#| label: ppl_plotprep
relpath <- "nogit/seqc100/purple/plot"
p <- dat$ppl_plots |>
  mutate(path = paste(relpath, .data$bname, sep = "/"))
```

## Circos

::: {layout-nrow=1}

```{r}
#| label: ppl_plots_circos
#| results: asis
cat_plot(p, "circos_baf_ar", group = "ppl_circos")
cat_plot(p, "circos_snv_indel_cn_sv", group = "ppl_circos")
```

:::

## QC Summary

```{r}
#| label: ppl_qc
#| classes: plain
qc_tab(dat)
```
